# security-dials.md (example)

1. **Allowlist:** Telegram bot only my user id (confirmed in gateway UI).
2. **Approvals:** smart (not YOLO). Dangerous shell still asks.
3. **Surface cut:** disabled unused MCP servers on this profile.

Logs: Desktop → logs / `hermes logs` (redact before sharing).
