# Dune Analytics — Web3 Dashboard Graphs Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Replace DeFiLlama text-only DEX sections with Dune-powered graphs styled to Delta V brand colors. Keep DeFiLlama TVL ranking as-is (it's clean).

**Architecture:** Extend existing `fetch-dune-dex-matrix.py` with 2 new queries. Build chart components using raw SVG (no chart library dependency). DeFiLlama TVL rankings stay — Dune replaces volume/trend sections.

**Queries confirmed working:**

| Query ID | Data | Rows | Use |
|----------|------|------|-----|
| 8029504 | Chain × Protocol cross-table | 75 | DEX Matrix (existing) |
| 8029496 | DEX TVL by chain | 10 | TVL (existing) |
| **8024914** | Per-chain volume + 24h delta | 12 | Volume bar chart + trend arrows |
| **7950834** | Weekly DEX volume (3 variants) | 208 | Line/area chart — 4yr history |
| 7992669 | (empty — needs params) | 0 | Skip for now |

**Tech Stack:** Python (urllib + Dune API), TypeScript/JSX (Next.js static export), raw SVG charts, Tailwind CSS Delta V brand tokens.

---

## Task 1: Extend fetch script with new queries

**Files:** `scripts/fetch-dune-dex-matrix.py`

Add two new queries, write output to `public/data/dex-metrics.json`:

```python
# Q8024914 — Per-chain volume + 24h delta
chain_vol_rows = execute_and_fetch(8024914, "chain-volume")

# Q7950834 — Weekly volume time series
weekly_rows = execute_and_fetch(7950834, "weekly-volume")
```

Output format:
```json
{
  "updated_at": "...",
  "source": "Dune Analytics",
  "chains": [{ "chain": "ethereum", "volume_24h": ..., "delta_pct": -5.5 }, ...],
  "weekly": [{ "week": "2022-07-18", "curated": ..., "filtered": ..., "raw": ... }, ...]
}
```

## Task 2: Build VolumeChart component

**Files:** `app/intelhub/components/VolumeChart.tsx`

SVG-based area/line chart with:
- X-axis: weekly timestamps (last 12 weeks)
- Y-axis: volume in billions
- Curated volume line (cyan)
- Filtered volume area fill (cyan/10)
- Delta V brand tokens: `var(--accent-cyan)`, `var(--bg-deep)`, `var(--text-muted)`
- Hover tooltip via `useChartHover`

Props: `data: { week: string; curated: number; filtered: number }[]`

## Task 3: Build ChainVolumeBar component

**Files:** `app/intelhub/components/ChainVolumeBar.tsx`

Horizontal bar chart:
- Top 8 chains by volume
- Bar width proportional to volume
- 24h delta shown as +green or -red arrow
- Chain name + volume formatted as `$X.XB`
- Brand colors: bars use `var(--accent-cyan)` gradient

Props: `data: { chain: string; volume_24h: number; delta_pct: number }[]`

## Task 4: Integrate into Web3Dashboard

**Files:** `app/intelhub/components/Web3Dashboard.tsx`

Replace the current DEX Dominance text section (lines 281-310) with:
```
┌──────────────────┬──────────────────┐
│  TVL Ranking     │  DEX Volume      │
│  (DeFiLlama)     │  (Dune Chart)    │
│  Top 8 chains    │  Weekly trend    │
│  table form      │  line/area chart │
└──────────────────┴──────────────────┘
│  Chain Volume Bars (Dune)              │
│  8 horizontal bars + 24h delta         │
└────────────────────────────────────────┘
```

**Keep:**
- TVL leaderboard (DeFiLlama) — clean, works
- Polymarket table — works
- Stablecoins section — works
- DEX Matrix table (Dune) — keep as-is below

**Replace:**
- DEX Dominance text table → Dune VolumeChart
- Current volume stats → ChainVolumeBar

## Task 5: Style — Delta V brand colors

Apply brand tokens consistently:
- `var(--accent-cyan)` for volume bars, chart lines
- `var(--accent-green)` for positive deltas
- `var(--accent-red)` for negative deltas
- `var(--bg-deep)` for chart backgrounds
- `var(--border-default)` for chart borders
- `var(--text-muted)` for axis labels
- `var(--text-primary)` for data labels

---

**Total: 5 tasks, ~60 min. DeFiLlama TVL stays. Dune replaces volume sections with graphs.**
