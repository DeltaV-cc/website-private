# Single-package workspace so pnpm can apply allowBuilds.
# Not a multi-package monorepo — only this root package is published/built.
packages:
  - "."

allowBuilds:
  sharp: true
  unrs-resolver: true
