node_modules/
.next/
out/
# Local wrangler state (deploy cache, telemetry) — never belongs in the repo
.wrangler/
.env
.env.*
!.env.example
*.log
.cache/
tsconfig.tsbuildinfo

# Codemod scratch (progress-key baseline + derived block census)
scripts/.keys-before.txt
scripts/.blocks-report.json
scripts/.density-proposals.json
__pycache__/
*.py[cod]
.Python
.DS_Store
Thumbs.db

# Stale static-export artifacts (real build output lives in out/, rebuilt by CI)
/_next/
/*.html
/*.txt

# Local reference material (not site source)
/_Ressources/

# Platform-specific AI skill installs
.claude/
.cursor/
.windsurf/
.kiro/
.factory/
.agents/
.continue/
.idea/
.vscode/

# ── INTERNAL DOCS — never commit ──
.hermes/
docs/archive/
content/wiki/
design-system/
UIUX-PROPOSAL.md
data/
# Site course curriculum (typed modules) lives under app/data/courses — must ship with the app.
!app/data/
!app/data/content-index.ts
!app/data/courses/
!app/data/courses/**


# ── Scripts with credentials/internal paths — keep local only ──
scripts/fetch-dune-dex-matrix.py
# scripts/refresh-data.py  # tracked again — pure public APIs, no secrets
scripts/sync-intel-to-site.py
scripts/add-to-watchlist.py
scripts/macro-pull.py
app/intelhub/sources-patch.txt

# Ops credentials / local intel state (never commit)
**/artemis-config.json
**/artemis-config.*.json
**/*stytch*token*
**/*stycth*
*.sqlite
*.sqlite3
intel.db
**/intel.db
